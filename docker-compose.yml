services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: drive_cartographer
      POSTGRES_USER: drive
      POSTGRES_PASSWORD: drive
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
